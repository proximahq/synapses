{
  "name": "proxima",
  "version": "1.0.7",
  "repository": {
    "type": "git",
    "url": "https://github.com/proximahq/synapses.git",
    "directory": "packages/proxima"
  },
  "bugs": "https://github.com/proximahq/synapses/issues",
  "homepage": "https://proxima.so",
  "main": "./dist/script.js",
  "source": "src/index.ts",
  "unpkg": "./dist/script.js",
  "types": "./src/types.d.ts",
  "scripts": {
    "build": "tsc --noEmit --lib ESNext,DOM  && tsup",
    "build:debug": "tsc --noEmit --lib ESNext,DOM  && tsup --env.DEBUG true ",
    "demo": "serve",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "get-port": "6.1.2",
    "msw": "0.49.2",
    "serve-handler": "6.1.5",
    "tsup": "8.0.1"
  },
  "test": {
    "environment": "happy-dom"
  }
}
